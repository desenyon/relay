# References

`$ref` expressions resolve to values in a session context. The specification is in
[`spec/RELAY_REFERENCE.md`](../spec/RELAY_REFERENCE.md). Use `relay.Session` and
`relay.reference.resolve_path` in Python.
