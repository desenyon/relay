# Delta messages

Delta frames mutate a base FULL message using SET, DEL, APP, and SPL operations.
See [`spec/RELAY_REFERENCE.md`](../spec/RELAY_REFERENCE.md) (delta section) and
the Python modules `relay.delta` and `relay.text_encoder` / `relay.text_decoder`.
