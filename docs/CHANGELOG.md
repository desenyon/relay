# Changelog

## 0.1.0

- Initial public Python package (`relay-format`) with binary encode/decode, text
  format, deltas, sessions, registry, CLI, and compatibility shims.
- TypeScript package scaffold (`relay-js`) with types, errors, schema helpers,
  and registry utilities; encoder/decoder parity is ongoing.
